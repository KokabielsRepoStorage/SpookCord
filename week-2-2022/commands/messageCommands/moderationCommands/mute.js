//! Eris does not support timeouts. I need to add that myself.
// module.exports = {
//     setup: async function setup(bot) {
//         const ping = bot.registerCommand("mute", async (msg, args) => {
//             const user = msg.mentions[0];


//         }, {
//             description: "Puts a user in timeout!",
//             usage: ".mute @user",
//         })
//     }
// }